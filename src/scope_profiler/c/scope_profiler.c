/* Compiled C99 backend for scope-profiler's native API.
 *
 * The implementation also lives in an includable header so C++17 projects
 * can select the header-only backend with SP_HEADER_ONLY.
 */
#include "scope_profiler_impl.h"
