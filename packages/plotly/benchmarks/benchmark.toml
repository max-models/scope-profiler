[benchmark]
name = "plotly-builders"
script = "profile.py"
runs = 5
warmups = 1
timeout_seconds = 120
only_user_code = true
output_dir = "../../../.scope-profiler"
threshold_pct = 2.0

[correctness]
command = ["npm", "test", "--prefix", ".."]
timeout_seconds = 120
